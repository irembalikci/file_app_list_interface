import 'package:flutter/material.dart';

class Info {
  int id;
  String titleText;
  String headerText;
  Icon icons;

  Info({
    required this.id,
    required this.titleText,
    required this.headerText,
    required this.icons,
  });
}